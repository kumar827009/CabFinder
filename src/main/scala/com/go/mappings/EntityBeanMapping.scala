package com.go.mappings

/*
import slick.ast.ColumnOption.AutoInc
import slick.lifted.Tag
import slick.model.{PrimaryKey, Table, Column}
import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent.{Future, Await}
import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent.duration.Duration
import scala.util.{Failure, Success}
import scala.slick.driver.MySQLDriver.simple._

import slick.lifted.ProvenShape
import slick.lifted.ForeignKey
*/

/**
 * Created by NKumar on 12/10/2016.
 */
class EntityBeanMapping {
/*  type Markers = (Int, String, Int, Int)

  class People(tag: Tag) extends Table[Markers](tag, "MARKERS") {
    def id = columns[Column](new Column("ID", PrimaryKey, AutoInc))

    def name = columns[String]("NAME")

    def age = column[Int]("AGE")

    def addressId = column[Int]("ADDRESS_ID")

    def * = (id, name, age, addressId)


  }

  lazy val people = TableQuery[People]*/
}
